# 다음 단계 가이드

**생성일시**: 2025-11-20 21:57:33

## ✅ 완료된 작업

1. ✅ `run_script.c` 생성 - 모든 스크립트 실행을 위한 setuid wrapper
2. ✅ `BindConfigService.java` 수정 - `PackageManagementService` 사용으로 통일
3. ✅ `ApacheConfigService.java` 수정 - `PackageManagementService` 사용으로 통일
4. ✅ 로그 경로 수정 - `/var/log/ensm/main/main-app.log`로 통일
5. ✅ `ensm-postinstall.sh` 생성 - RPM 설치 후 초기 설정 스크립트
6. ✅ 불필요한 폴더 삭제 - `ensm-scripts/ensm/` 제거
7. ✅ 스펙 파일 정리 - `install_package.c`, `enable_service.c` 관련 코드 제거

---

## 📋 다음 단계

### 1. 빌드 스크립트 재구성 (우선순위: 높음)

**현재 상황**:
- Gradle 빌드 설정이 삭제되어 있음
- RPM 스펙 파일에서 JAR 파일을 참조하지만 생성 방법 없음

**필요한 작업**:
- Gradle 빌드 설정 재구성 (`build.gradle`, `settings.gradle`)
- 또는 빌드 스크립트 작성 (JAR 파일 생성)
- `ensm-auth`와 `ensm-main` 모두 빌드 가능하도록 설정

**옵션**:
1. **Gradle 멀티 프로젝트 설정** (권장)
   - 루트 `settings.gradle`에 두 프로젝트 포함
   - 각 프로젝트별 `build.gradle` 설정
   - 프론트엔드 빌드 통합

2. **간단한 빌드 스크립트**
   - `build.sh` 또는 `build.bat` 작성
   - 각 프로젝트를 개별적으로 빌드
   - JAR 파일을 RPM SOURCES로 복사

---

### 2. 최종 검증 (우선순위: 중간)

**확인 사항**:
- [ ] 모든 스크립트 파일 존재 확인
- [ ] 백엔드 코드에서 잘못된 경로 참조 확인
- [ ] API 엔드포인트 매칭 확인
- [ ] 프론트엔드 경로 확인

---

### 3. 문서화 (우선순위: 낮음)

**필요한 문서**:
- 빌드 가이드
- 배포 가이드
- 개발 환경 설정 가이드

---

## 🎯 권장 다음 작업

**1순위: 빌드 스크립트 재구성**
- 프로젝트를 빌드할 수 있어야 RPM 패키징 가능
- Gradle 설정 또는 빌드 스크립트 필요

**2순위: 최종 검증**
- 모든 파일이 올바르게 연결되어 있는지 확인
- 누락된 파일이나 잘못된 경로 확인

---

## 💡 제안

지금 바로 진행할 수 있는 작업:
1. **Gradle 빌드 설정 재구성** - 프로젝트 빌드 가능하도록
2. **빌드 스크립트 작성** - 간단한 빌드 방법 제공
3. **최종 점검** - 모든 것이 올바르게 연결되어 있는지 확인

어떤 작업부터 진행할까요?

